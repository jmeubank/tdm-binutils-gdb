Though GOLD is not yet enabled for TDM-GCC, this patch will help

From: J.M. Eubank <john@thesnappy.net>

From
   <https://github.com/msys2/MINGW-packages/blob/master/mingw-w64-binutils/0001-enable-gold-on.mingw32.patch>
---
 configure |    7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

diff --git a/configure b/configure
index 91dc42f6..4e7a6955 100755
--- a/configure
+++ b/configure
@@ -3051,6 +3051,7 @@ case "${ENABLE_GOLD}" in
   yes|default)
     # Check for ELF target.
     is_elf=no
+    is_pe=no
     case "${target}" in
       *-*-elf* | *-*-sysv4* | *-*-unixware* | *-*-eabi* | hppa*64*-*-hpux* \
       | *-*-linux* | *-*-gnu* | frv-*-uclinux* | *-*-irix5* | *-*-irix6* \
@@ -3062,10 +3063,12 @@ case "${ENABLE_GOLD}" in
           *)
             is_elf=yes
             ;;
-        esac
+        esac;;
+      *-*-mingw*)
+        is_pe=yes;;
     esac
 
-    if test "$is_elf" = "yes"; then
+    if test "$is_elf" = "yes" -o "$is_pe" = "yes"; then
       # Check for target supported by gold.
       case "${target}" in
         i?86-*-* | x86_64-*-* | sparc*-*-* | powerpc*-*-* | arm*-*-* \
